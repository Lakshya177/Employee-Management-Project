# Employee-Management-Project
Employee Management System is a desktop application built using Java Swing and JDBC that allows admin to add, update, delete, and search employee records. It includes auto-generated employee ID and input validation for mobile number, email, and empty fields, with MySQL database connectivity.
